# AI provider
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# Sending (MVP single inbox; you can add more later)
SMTP_HOST=smtp.yourdomain.com
SMTP_PORT=587
SMTP_USERNAME=outreach@yourdomain.com
SMTP_PASSWORD=your_app_password
SMTP_FROM_NAME=Chris from Walkers
SMTP_FROM_EMAIL=outreach@yourdomain.com

# Reply tracking (IMAP)
IMAP_HOST=imap.yourdomain.com
IMAP_USERNAME=outreach@yourdomain.com
IMAP_PASSWORD=your_app_password
IMAP_FOLDER=INBOX

# Optional
RATE_PER_MIN=12
